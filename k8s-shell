#!/usr/bin/env bash
# Author: github.com/danielhoherd
# License: Unlicense
# Spawn a new container and drop into a shell

readonly IMAGE="$1"
shift

epochtime="$(date +%s)"

kubectl run --restart=Never --rm -t -i "${USER//./-}-test-${epochtime}" --image="${IMAGE:-danielhoherd/uw}" "${@:-bash}"
